# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build try-zod`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t try-zod`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system try-zod && \
          adduser --system -G try-zod try-zod

COPY dist/apps/try-zod try-zod
RUN chown -R try-zod:try-zod .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix try-zod --omit=dev -f install

CMD [ "node", "try-zod" ]
